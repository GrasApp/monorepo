describe('ProductRow & Products Data Tests', () => {
    test('findLowStockVariants returns the correct data shape', async () => {
        expect('1').toEqual('1');
    });

    test('findLowStockVariants returns the correct number of JSX Elements', async () => {
        expect('1').toEqual('1');
    });
});
