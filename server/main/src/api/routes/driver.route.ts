import { Router } from "express";
import { driverCtrl } from "../controllers";
const router = Router();
/* =================================
Driver Routes

================================= */

export default router;
